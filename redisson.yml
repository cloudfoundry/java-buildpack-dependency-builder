---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: cfje/java-buildpack-dependency-builder
    tag: latest
    username: ((docker-username))
    password: ((docker-password))

inputs:
- name: java-buildpack-dependency-builder
- name: redisson-all-archives
- name: redisson-tomcat-8-archives
- name: redisson-tomcat-9-archives
- name: redisson-tomcat-10-archives

outputs:
- name: repository

run:
  path: java-buildpack-dependency-builder/redisson.sh
