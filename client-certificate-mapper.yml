---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfje/java-buildpack-dependency-builder
    tag: latest

inputs:
- name: java-buildpack-dependency-builder
- name: client-certificate-mapper-archives

outputs:
- name: repository

run:
  path: java-buildpack-dependency-builder/client-certificate-mapper.sh
