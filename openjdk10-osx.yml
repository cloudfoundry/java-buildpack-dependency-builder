---
platform: darwin

inputs:
- name: cacerts-keystores
- name: java-archives
- name: java-buildpack-dependency-builder

run:
  path: java-buildpack-dependency-builder/openjdk10.sh

params:
  PLATFORM:       mountainlion
  XCODE_LOCATION: /Users/pivotalcf/concourse/Xcode8.app

  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  CLOUDFRONT_DISTRIBUTION_IDS:
  DOWNLOAD_DOMAIN:
  S3_BUCKET:
