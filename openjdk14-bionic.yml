---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: cfje/openjdk
    tag: cflinuxfs3

inputs:
- name: cacerts-repository
- name: java-buildpack-dependency-builder
- name: java-14-release

outputs:
- name: images

run:
  path: java-buildpack-dependency-builder/openjdk14.sh

params:
  JAVA_HOME: /opt/openjdk-13
